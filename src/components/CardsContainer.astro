<div class="cards-container">
  <img
    src="/images/svg/cards-decoration.svg"
    alt="Decoración floral"
    class="decoration top-left"
  />
  <img
    src="/images/svg/cards-decoration-2.svg"
    alt="Decoración floral"
    class="decoration bottom-right"
  />

  <slot />
</div>

<style>
  .cards-container {
    position: relative;
    display: flex;
    gap: 2rem;
    justify-content: center;
    width: 100%;
    max-width: 1200px;
    z-index: 2;
  }

  .decoration {
    position: absolute;
    width: 80px;
    height: auto;
    z-index: 1;
    opacity: 0;
    transform: translateY(20px);
  }

  .top-left { top: -50px; left: -40px; }
  .bottom-right { bottom: -50px; right: -40px; }

  :global(.animate) .top-left {
    animation:
      deco-enter 0.8s ease-out forwards,
      floatAnimationTop 2s ease-in-out infinite 0.8s;
  }
  :global(.animate) .bottom-right {
    animation:
      deco-enter 0.8s ease-out forwards 0.2s,
      floatAnimationBottom 2s ease-in-out infinite .3s;
  }

  /* --- ANIMACIONES --- */

  @keyframes deco-enter {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes floatAnimationTop {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  @keyframes floatAnimationBottom {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(10px); }
  }

  /* --- MEDIA QUERIES --- */

  @media (max-width: 900px) {
    .cards-container {
      flex-direction: column;
      gap: 1.5rem;
    }
    .decoration {
      width: 60px;
    }
    .top-left { top: -20px; left: -20px; }
    .bottom-right { bottom: -20px; right: -20px; }
  }
</style>